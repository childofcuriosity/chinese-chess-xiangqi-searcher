# 模板: 复制本文件为 secrets.env 并填入真实值 (secrets.env 已被 gitignore)
# ssh 别名 (配置在 ~/.ssh/config)
SERVER=your-ssh-alias
# 服务器上的部署目录 / systemd 服务名 / 端口
REMOTE_DIR=/opt/xq
SERVICE=xiangqi-web
PORT=8100
# 朋友访问的公网地址
PUBLIC_URL=http://YOUR_SERVER_IP:8100
